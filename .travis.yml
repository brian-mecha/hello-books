language: python

python:
    - 3.5.2

# install dependencies for development environment
install:
    - pip install -r requirements.txt
    - pip install coveralls

script:
    - py.test --cov

after_success:
    - coveralls

